services:
  - type: web
    name: discord-bot
    env: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
    disks:
      - name: bot-data
        mountPath: /opt/render/project/src/data
        sizeGB: 1
